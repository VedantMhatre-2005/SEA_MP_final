[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
	slow: end-to-end or browser-level tests that require a running frontend and backend
	whitebox: unit tests that exercise internal branches and control flow
